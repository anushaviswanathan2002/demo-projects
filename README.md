# Demo Projects

## CounterApp

A simple interactive .NET 8 console counter application.

### Features
- Increment the counter (`+`)
- Decrement the counter (`-`)
- Reset the counter to zero (`r`)
- Quit the application (`q`)

### Run

```bash
cd CounterApp
dotnet run
```
